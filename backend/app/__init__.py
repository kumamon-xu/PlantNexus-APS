"""PlantNexus APS executable-specification package metadata.

P1 publishes strict canonical data contracts, normalization rules, and the
independent canonical-data quality gate; expansion, Snapshot builders, and
Solver remain separately gated.
"""

CODE_VERSION = "0.0.0"
SPEC_VERSION = "0.3.0"
SCHEMA_VERSION = "2.2.0"

__all__ = ["CODE_VERSION", "SCHEMA_VERSION", "SPEC_VERSION"]
