# P4 continuous disruption replay calculation note

`SIM-P4-DISRUPTION-REPLAY-001@1.0.0` is a bounded correctness asset for TASK-P4-10. It uses seed `20260828`, a one-second virtual-clock resolution, a `2026-11-06T10:52:00Z` origin, and a 900-second Simulation-only freeze window. Its standard Raw Staging base maps four generated resource states to the ExecutionEvent vocabulary value `AVAILABLE` and binds one explicit HARD lock to `2026-11-06T12:20:00Z`–`12:25:00Z`; the resulting Snapshot and Problem fingerprints are recorded exactly in the asset.

The single stream has five ordered steps and eight standard `execution-event.v1` events: urgent demand at 60 seconds; machine unavailable/recovered at 120/180; material delayed/ready at 240/300; processing duration/remaining observations at 360/420; and early completion at 480. Distinct offsets make the step boundary independent of declaration order while the simulator still applies its named-child-seed queue rule.

Every step must preserve completed/running/HARD/freeze facts, produce a fresh Validator PASS and complete ChangeReport, and advance the next test baseline only through a distinct PUBLISHED-shaped carrier with the exact DRAFT content, an explicit `source_draft_id`, and `SIMULATION_NON_PRODUCTION`/no-authority markers. The urgent quantity `4`, priority `10`, event times, observed duration `1200`, remaining duration `1200`, and freeze window `900` are synthetic test values registered by TASK-P4-10. Same-seed replay compares the exact event stream and a semantic result projection while retaining raw Solver timestamps and timing observations. These values do not define a Production distribution, policy, authority, capacity, or SLA.
