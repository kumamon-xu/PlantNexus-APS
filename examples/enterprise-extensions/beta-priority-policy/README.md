# Beta priority-policy Extension

Synthetic reference owned by `PlantNexus Synthetic Reference Beta`. It provides
an integer-only enterprise tie-break Objective, a deterministic non-feasibility
Planning Rule, a Replan Policy that echoes all protected bindings, and a Plugin
Registry that delegates to the SDK's code-free resolution.

This project does not import Alpha, APS Core, Solver internals, Validator
internals, HTTP, database, filesystem, or network code.
