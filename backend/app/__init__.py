"""PlantNexus APS executable-specification package metadata.

P1 publishes strict canonical data contracts while the import pipeline,
schedule validator, and Solver behavior remain separately gated tasks.
"""

CODE_VERSION = "0.0.0"
SPEC_VERSION = "0.3.0"
SCHEMA_VERSION = "2.0.0"

__all__ = ["CODE_VERSION", "SCHEMA_VERSION", "SPEC_VERSION"]
