# P9 qualification v1 calculation note

Synthetic-only; no Production binding. Assumptions SIM-ASSUMPTION-027/028.

The catalog copies the frozen P2 XS/S/M profile shape: 4/8/12 orders, 2/3/4 operations per order, 3/6/8 resources, two candidates per operation, 1/2/4 calendar fragments, 180/480/900 horizon ticks and 60 seconds per tick. Solver limits remain 3/5/4 seconds. Development seeds are 910101/910102/910103; sealed holdout seeds are 910901/910902/910903.

For each named label, take the first eight bytes of SHA256(`p9-qualification-catalog.v1|B01|seed|label`) as an unsigned big-endian integer, then modulo 4 for `<job>/due` or modulo 3 for `<job>/<operation>/<resource>/duration`. Add that many ticks to the copied blueprint. Labels are independent of call order. The P2 blueprint generator and source assembler plus P1 normalization remain unchanged; the outer record names the composite P9 generator.

No expected optimal schedule is generated by the Solver. B01 replays existing independently calculated P2 Golden and illegal-candidate mutation tests. Generated cases require a valid candidate, independent Validator PASS and comparison with five existing Reference schedulers. A corrupted resource reference must be rejected. Equivalent optimal assignment ordering is not a determinism requirement; canonical input and Problem hashes are.

The negative catalog blocks all calendar availability for development XS (expected INFEASIBLE) and gives development M a 1e-9-second solve budget (expected NO_SOLUTION_WITHIN_LIMIT). Both must produce zero candidates. These declarations precede execution and never use holdout outcomes to tune thresholds.

Each positive profile runs one warmup and three measured repetitions: nine development and nine holdout observations. Six warmups are separate; the two negative cases give a total outcome denominator of twenty. Raw Solver objective/bound/gap, independent validation, complexity, Reference outputs, min/median/max timings, Solver memory and process peak RSS are retained. No small-sample P95, production SLA or prediction accuracy is inferred.

Development max × 2.5, with predeclared one-second stage/four-second end-to-end/one-MB memory floors, is frozen before the append-only holdout access receipt. Each new environment calibrates independently and cannot claim historical speed comparisons. The committed baseline records the initial Windows observation; CI records its own development-frozen Linux baseline. Failed directories and first holdout observations must remain available; no in-place baseline update is permitted.

B01–B10 owner suites run fresh and record exact JUnit names. Generated real TCP/Redis/Worker measurements and owner contract regressions are distinct evidence. This catalog does not add PROFILE-A–E, L/XL, enterprise live data, concurrent throughput or a released Runtime/SDK/Kit combination.

## Initial qualification finding

The first complete Windows holdout observation is NOT_READY: all three measured M candidates are FEASIBLE and independently valid, but weighted tardiness is 14,940 seconds versus the best Reference result of 3,900 seconds under the frozen four-second solve limit. The quality gate blocks acceptance. The development-derived baseline is retained for reproducibility; its existence does not mean qualification passed. Do not tune this exposed holdout, increase its budget or relabel it as a clean new holdout. Solver corrective work and a new independent holdout require a separately bounded decision.

## P9-09 corrective qualification v2

The initial v1 qualification failed quality and remains immutable evidence. ADR-0021 adds bounded backend-local dispatch hints without changing the hard model, objective, limits or independent Validator. Catalog/holdout-seal v2 and SIM-ASSUMPTION-029 were recorded before Solver tuning: development seeds and the SHA child formula remain unchanged; independent holdout seeds are 920901/920902/920903. New development observations freeze a versioned v2 budget before holdout access; no threshold is derived from holdout. The runner defaults to v2; `--catalog-version v1` explicitly emits EXPOSED_V1_FAILURE_REGRESSION, never independent qualification. Both old regression and new qualification are required in CI. Original v1 budgets and failed reports remain available; no Production capacity/SLA inference.
